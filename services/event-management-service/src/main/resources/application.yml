server:
  port: 8081

spring:
  application:
    name: event-management-service

  datasource:
    url: jdbc:postgresql://localhost:5432/event_management_db
    username: postgres
    password: 275@ash112

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

logging:
  level:
    org.flywaydb: DEBUG
