name: Certificate Service Pipeline

on:
  push:
    branches: ["feature-certificate-service-backend"]
    paths:
      - "services/certificate-service/**"
      - "pom.xml"
  pull_request:
    branches: ["main"]
    paths:
      - "services/certificate-service/**"
      - "pom.xml"

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Clone Repository
        uses: actions/checkout@v7

      - name: Install JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: "21"
          distribution: "temurin"
          cache: "maven"

      - name: Give Permissions to Maven in ubuntu
        run: chmod +x mvnw

      #      to see what files are in the project folder
      - name: see files
        run: ls -la

      - name: Build & Test Certificate Service Service
        run: ./mvnw clean test -pl services/certificate-service -am

      - name: Package Certificate Service Service
        run: ./mvnw package -DskipTests -pl services/certificate-service -am

      - name: Upload Certificate Service Service Artifact
        uses: actions/upload-artifact@v4
        with:
          name: certificate-service-jar
          path: services/certificate-service/target/*.jar
