name: User Profile Service Pipeline

on:
  push:
    branches: ["user-profile-service"]
    paths:
      - "services/user-service/**"
      - "pom.xml"
  pull_request:
    branches: ["main"]
    paths:
      - "services/user-service/**"
      - "pom.xml"

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Clone Repository
        uses: actions/checkout@v7

      - name: Install JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: "21"
          distribution: "temurin"
          cache: "maven"

      - name: Give Permissions to Maven in ubuntu
        run: chmod +x mvnw

      #      to see what files are in the project folder
      - name: see files
        run: ls -la

      - name: Build & Test User Profile Service
        run: ./mvnw clean test -pl services/user-service -am

      - name: Package User Profile Service
        run: ./mvnw package -DskipTests -pl services/user-service -am

      - name: Upload User Profile Service Artifact
        uses: actions/upload-artifact@v4
        with:
          name: user-service-jar
          path: services/user-service/target/*.jar
